# ─────────────────────────────────────────────────────────────────────────────
# Lightweight Crack Segmentation — Python dependencies (venv / pip)
# Python 3.11 recommended.
#
# IMPORTANT — install PyTorch FIRST, separately (it is NOT listed below because
# it needs a CUDA-specific index):
#
#   # RTX 50-series (Blackwell, sm_120) — CUDA 12.8 nightly:
#   pip install --pre torch==2.12.0.dev20260327+cu128 torchvision==0.26.0.dev20260326+cu128 \
#       --index-url https://download.pytorch.org/whl/nightly/cu128
#
#   # Stable GPUs (CUDA 12.1) — use a stable release instead:
#   pip install torch torchvision
#
# Then:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

numpy>=1.24,<2.0
opencv-python==4.9.0.80
pillow>=10.0
albumentations==1.4.21
torchmetrics>=1.2
scikit-image>=0.21
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
pyyaml
tqdm
loguru
tensorboard
